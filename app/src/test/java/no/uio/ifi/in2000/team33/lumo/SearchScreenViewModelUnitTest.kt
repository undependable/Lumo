package no.uio.ifi.in2000.team33.lumo

/*
class SearchScreenViewModelUnitTest {

    //TDD - Relatert til ViewModel
    @Test
    fun `loadAverageSSBPrice should update MapPoint with valid price`() = runBlocking {
        // Arrange
        val viewModel = MapPointViewModel()
        val testMapPoint = MapPoint(
            name = "Oslo",
            lat = 59.91,
            lon = 10.75,
            erHus = true,
            postnummer = "0001",
            poststed = "Oslo",
            region = "NO1"
        )

        // Act
        val updatedMapPoint = viewModel.loadAverageSSBPrice(testMapPoint)

        // Assert
        println("Årlig pris: ${updatedMapPoint.annualPrice}")
        assertTrue("Annual price should be a numeric value", updatedMapPoint.annualPrice.toDoubleOrNull() != null)
        assertTrue("Annual price should be greater than 0", updatedMapPoint.annualPrice.toDouble() > 0)
    }
}*/